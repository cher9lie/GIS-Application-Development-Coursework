# 作业 8：空间查询与分析

## 已实现功能

- 按 `NAME` 字段进行精确属性查询并高亮结果
- 在地图上拉框执行空间选择，并把属性显示在表格中
- 以选中要素生成 500 米缓冲区并加载结果

打开 `GISDev8.sln`，使用 ArcGIS Engine 10.2、.NET Framework 4.0、x86 编译。属性查询要求目标图层存在 `NAME` 字段。生成缓冲区时程序会让使用者选择输出 Shapefile，不再写死到 `C:\temp`；距离按 500 米设置，输入数据应使用合适的投影坐标系，否则距离含义可能不准确。
